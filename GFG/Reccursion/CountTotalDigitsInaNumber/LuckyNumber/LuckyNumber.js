/**
 * at first step every second element is deleted so 
    n/2 elements are deleted  so n-n/2 are left 
    and 2nd step every third element is deleted so n/3 elements are deleted and remaining are n-n/3  and so on 
    every
 */
